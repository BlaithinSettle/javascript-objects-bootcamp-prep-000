var playlist={artistName: 'songTitle'};
function updatePlaylist(playlist, artistName, songTitle){
    artistsName[songTitles]=playlist
return playlist;
}

function removeFromPlaylist(obj, artistName){
    delete playlist['artistName'];
  
}